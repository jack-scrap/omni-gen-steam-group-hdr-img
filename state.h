#pragma once

#include <string>
#include <GL/glew.h>
#include <glm/glm.hpp>

#include "math.h"

namespace state {
	static unsigned int
		line = 18,
		ln = 36;

	static unsigned int fps = 15;

	static unsigned int tabWd = 2;

	static std::string format = "%Y-%m-%d %H:%m";
}
