`import omni`

# Data
`omni.data`

# Vehicles
## Crane
### Navigation
`zoom(delta)` - Move forward, back
`pan(delta)` - Translate head left and right
`ped(delta)` - Translate head up and down

`grab()` - Grab container

## Truck
This allows for complete freedom of movement without being restricted to an axis.

`mv(delta)` - Move forward, back

`turn(delta)` - Turn left, right

## Ship
`zoom(delta)`

# Data
Data can be accessed.

Both initial and the desired state is provided.

`.data`

`._sz`
`._x`
`._y`
`._z`
`._loc`

# Bounds
Bounds are provided.

`.bounds`

These can be used to assert.
